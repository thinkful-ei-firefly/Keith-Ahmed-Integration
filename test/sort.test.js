const {sort} = require('../app');
const chai = require('chai');
const expect = chai.expect;

/*describe("sort function", () => {
  it('should sort a array of positive numbers', () => {
    let list = [1,5,30,15,4];
    let sorted = [1,4,5,15,30];
    list = sort(list);
    expect(list).to.deep.equal(sorted);
  });
});*/