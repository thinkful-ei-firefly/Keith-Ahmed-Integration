const {divide} = require('../app');
const chai = require('chai');
const expect = chai.expect;

/*describe("Divide function", () => {
  it('Should divide positive ints correctly', () => {
    let a = 8;
    let b = 4;
    const expectedAnswer = 2;
    const c = divide(a,b);
    expect(c).to.equal(expectedAnswer);
  });
  it('Should throw an error when divide by 0', () => {
    let a = 8;
    let b = 0;
    const fn = () => {divide(a,b)};

    expect(fn).to.throw();
  });
});*/